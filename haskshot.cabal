cabal-version: 3.0
name: haskshot
version: 8.10

executable haskshot
    main-is: Main.hs
    build-depends:
        base >= 4.8,
        binary >= 0.8,
        bytestring >= 0.10,
        X11 >= 1.10,
        containers >= 0.6,
        JuicyPixels >= 3.3,
        primitive >= 0.7
